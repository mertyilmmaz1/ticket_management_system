/// Tablet breakpoint and layout constants.
/// Min touch target ~48dp per plan.
class AppConstants {
  AppConstants._();

  /// Tablet breakpoint (width in logical pixels).
  static const double tabletBreakpoint = 600;

  /// Minimum touch target size (dp).
  static const double minTouchTarget = 48;

  /// Default padding for tablet screens.
  static const double screenPadding = 24;

  /// Grid cross-axis count for table cards on home.
  static const int tablesGridCrossCount = 3;
}
